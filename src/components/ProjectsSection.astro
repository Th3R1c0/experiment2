---
// export interface Props {
//   title: string;
// }

// const { title } = Astro.props;
import Cards from "./Card";
import { cva } from "class-variance-authority";
const button = cva("px-4 py-2 text-white  rounded-md",{
    variants: {
        intent: {
            primary: 'rounded-md border-white border-2 p-4'
        }
    }
})

const text = cva('', {
    variants: {
        type:{
            header: 'text-white text-5xl font-bold letter-spacing-2'
        } ,
    }
})
---

<div class="flex flex-col items-center w-full h-screen bg-black">
    <div className={text({type: 'header'})}>Projects Section</div>
    <Cards client:load />
    
  </div>
    
  </div>
  